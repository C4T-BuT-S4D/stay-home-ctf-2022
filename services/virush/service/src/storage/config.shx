#!/usr/bin/shx

@module storage/config || {
    @require logging

    if ! (std::hasvar STORAGE_PATH); then
        log::error "STORAGE_PATH is not defined"
        std::raise
    fi

    declare -rg \
        StoragePath=$STORAGE_PATH \
        StorageDataLength=1024
}
